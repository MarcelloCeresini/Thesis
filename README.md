# Thesis

## Setup the virtual environment with base dependencies

Run setup.py, it will create a virtual environment named "env_Thesis"

Then activate the env with

```bash
source env_Thesis/bin/activate
```

and run

```bash
pip install -r requirements.txt 
```
